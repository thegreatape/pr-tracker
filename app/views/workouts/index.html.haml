%h1.title.is-2 Workouts

= render "date_pagination"

- @dates.each do |date|
  .block
    = turbo_frame_tag date_frame_id(date) do
      - if workout = @workouts[date]&.first
        = render workout
      - else
        = render partial: "new", locals: {date: date}

= render "date_pagination"
