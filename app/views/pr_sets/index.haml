%h1 PRs

- sets_by_exercise = @pr_sets.group_by {|pr_set| pr_set.exercise.name }

- PrSetsController::BENCHMARK_LIFTS.each do |lift|
  %h2= lift
  %table
    %thead
      %tr
        %th Reps
        %th Weight (lbs)
        %th e1RM
        %th Date
    %tbody
      %tr
        - (1..20).each do |reps|
          - lift_sets = sets_by_exercise[lift]
          - matching_pr_set = lift_sets.find {|s| s.reps == reps}
          %tr
            %td= reps
            %td= number_to_human(matching_pr_set&.weight_lbs, strip_insignificant_zeros: true)
            %td= matching_pr_set&.e1rm
            %td= matching_pr_set&.date




